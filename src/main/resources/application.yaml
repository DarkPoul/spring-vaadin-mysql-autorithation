spring:
  application:
    name: dekanat

  datasource:
    username: root
    password: toor
    url: jdbc:mysql://localhost:3306/dekanat

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

